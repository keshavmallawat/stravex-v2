/**
 * Site-wide footer component.
 *
 * Responsibilities:
 * - Full sitemap (all 7 pages + product/technology sub-pages)
 * - Legal links (Privacy, Terms)
 * - Registered entity info (Indian company registration — signals legitimacy)
 * - Social / press contact links
 * - Optional: single clean newsletter signup field (no spam patterns)
 *
 * Treated as a secondary navigation and trust layer, not filler.
 * Architecture-driven, not an afterthought.
 */

export function Footer() {
  return <footer>{/* TODO: Sitemap, legal, entity info, social/contact, newsletter */}</footer>;
}
